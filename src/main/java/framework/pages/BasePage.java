package framework.pages;

import java.util.logging.Logger;

public abstract class BasePage {
    // logging
    protected Logger log = Logger.getLogger(this.getClass().getName());

    public BasePage() {
        // TODO: webdriver need to be implemented.
    }
}
